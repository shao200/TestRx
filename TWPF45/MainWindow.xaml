<controls:MetroWindow x:Class="TWPF45.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition  Height="1*" />
        </Grid.RowDefinitions>
            <StackPanel>
                <TextBox Name="QueryWord"/>
                <Button Name="QueryButton"  Content="Query"/>
            <ProgressBar Name="IsBusy" IsIndeterminate="True"/>
        </StackPanel>

        <ListBox Grid.Row="1" Name="QueryResults"  >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"  Height="16" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</controls:MetroWindow>
